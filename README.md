# user-sim
Simulated usability testing with Claude: generates a cohort of distinct synthetic personas, walks each through your product's core flow, and synthesizes friction points and ranked design fixes. Single-file web app, no build step.
